static def getGithubToken() {
    File file = new File("../github_token.txt")
    String token
    if (file.exists()) {
        token = "\"${file.text}\""
    } else {
        token = "\"\""
    }
    return token
}

ext.getGithubToken = this.&getGithubToken
